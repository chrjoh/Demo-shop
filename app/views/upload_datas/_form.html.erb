<%= form_for(@upload_data,:html => { :multipart => true }) do |f| %>
  <% if @upload_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@upload_data.errors.count, "error") %> prohibited this upload_data from being saved:</h2>

      <ul>
      <% @upload_data.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :image %>
  <%= f.file_field :image %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
